# Historical Data and Backtest Dataset Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

This worktree is assigned the fixtures/tests preparation slice for Chunk 1 only.

Focus:
- `trading_system/tests/test_backtest_archive_runtime_bundle.py`
- `trading_system/tests/fixtures/archive_runtime/`
- archive path/type/runtime-bundle test scaffolding
- no production feature expansion beyond what is needed to keep fixture/test shape aligned
