# 2026-03-23

- 用户新增长期规则：以后所有 Codex 任务默认 `thinking level = high`，除非老板明确要求改口。
- 该偏好已同步写入 `MEMORY.md`，作为长期默认执行标准。
- 当前继续中的 `trading_system` 开发任务也按这条规则执行与汇报。
- 用户新增长期要求：以后每轮开发/执行状态都要用通俗易懂的大白话说明本轮具体改了什么、用途是什么、验证结果怎样。
- 用户新增长期要求：以后每次回复都要明确说明本轮实际调用了哪些 skill；如果本轮没调用 skill，也要直接说没调用。
- 用户在本次 `trading_system` 开发中临时要求：先不要使用 Codex；当前阶段改为 Claw 直接在主工作区实现，除非老板后续重新允许。
- `trading_system` 主线已从零散 `paper_verification` guardrail 补丁切换为路线图驱动；当前路线图文档为 `docs/superpowers/plans/2026-03-23-trading-system-p0-p1-p2-roadmap.md`。
- 路线图已落地并提交：`7ca38e1`（`docs: add trading system P0 P1 P2 roadmap`）。
- 路线图后的 P0.1 已启动并已落地多步执行边界：`d99a49c`（显式 execution mode 边界）、`de9c2f4`（dry-run executor 无副作用）、`5cb3fb5`（dry-run main cycle 无副作用）、`9fb6650`（executor 强制遵守 execution mode）。
