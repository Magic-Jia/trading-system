# 2026-03-17

- 用户新增长期要求：只要 Claw 使用了 Codex，必须在回复中明确告知是否使用、所用模型、所用思考深度（thinking level）。
- 用户进一步升级要求：所有写代码任务，无论大小，都默认交给 Codex；不要因为任务小就绕过 Codex。
- 用户决定默认采用 Codex 方案 2（隔离开发型）：先在隔离目录 / worktree 中运行 Codex，再审查并合并回主项目。
- 本次关于 trading_system 目录骨架与 MVP 架构文档的修改，未使用 Codex；由 Claw 直接在工作区完成。
- 已确认 Codex 需同时配置凭证来源与路由：`~/.codex/auth.json` 负责 API key，`~/.codex/config.toml` 负责 provider/base URL；当前已配置为走 `https://api.yescode.cloud/v1`，provider=`proxy`，`requires_openai_auth=true`，并已最小验证通过。
- 已在 `~/.profile` 中显式 `unset OPENAI_API_KEY`，避免 Codex 被环境变量干扰；后续默认依赖 `auth.json + config.toml`。
- 自动交易程序已新增 portfolio management MVP：持仓同步（`portfolio/positions.py`）与持仓管理建议（`portfolio/lifecycle.py`），可输出 `ADD_PROTECTIVE_STOP` / `BREAK_EVEN` / `PARTIAL_TAKE_PROFIT` / `EXIT` 建议，并已提交 `Add trading system portfolio management MVP`。
- 本轮恢复开发已按用户要求实际使用 Codex，并走隔离 worktree 流程；使用模型 `gpt-5.4`、思考深度 `high`。
