- 用户明确纠正：当 Claw 说“我现在去做 / 我现在开始做 / 继续推进”时，默认含义必须是已经开始执行，不需要老板再次说“继续”；如果还需要用户二次授权，前述表述就不成立。
- 用户连续多次指出：长时间后台开发时，若需要用户来追问进度，说明进度汇报机制失败；“有新 commit 会主动汇报”必须严格执行，不能等用户来问后再汇报。
- 用户进一步指出：所谓“固定心跳”如果没有 OpenClaw heartbeat/cron 之类的外部触发器，就不是真正机制；后续必须用 OpenClaw heartbeat 来触发开发进度心跳，而不是仅靠口头规则或自觉。
- 已把开发进度 heartbeat 的触发逻辑写入 `HEARTBEAT.md`：当 `memory/dev-status.md` 显示有活跃开发任务且距离上次用户更新超过 45 分钟时，应主动发送固定格式进度；否则回复 `HEARTBEAT_OK`。
- 交易系统开发主线已并入：P0 已完成并通过主工作区完整测试；rotation engine 第一版及其 runtime/reporting/state integration 也已并回主线。主线新增的关键提交包括 `51753ba`（rotation candidates）、`6a7f1ed`（rotation runtime/reporting summary）、`7d434c5`（rotation runtime outputs docs）、`f45d7f2`（preserve rotation execution summary on replay）。
- 当前后续开发仍默认使用 Codex 方案 2（隔离 worktree），模型 `gpt-5.4`，thinking level `high`；用户已授权除非遇到必须其拍板的事项，否则应持续推进而不要停下来等待确认。
- 新增开发执行硬规则：当老板说“继续开发”时，默认表示任务必须重新进入主动执行态；若 Codex 没真正跑起来、秒退、或当前没有 active executor，必须立即明说。对于持续开发任务，若 Codex 启动失败/环境阻塞，必须立即汇报并立刻在“换可行路径重试 / 切备用路径 / 明确当前无人执行且缺什么条件”三者中择一；同时增加最小启动预检（目标路径、`git status`、最小 Codex 启动），避免把未跑起来的任务误报为“继续推进中”。
- 新增汇报格式硬规则：以后所有开发状态汇报都必须明确写出 Codex 当前是“运行中”还是“未运行”；若未运行，必须进一步说明是正常结束、启动失败、崩溃，还是当前没有 active executor。
- 再加一条退出即汇报的硬规则：只要后台 Codex 任务退出（正常结束 / 失败 / 崩溃 / 会话丢失），必须立即主动发状态，不能等老板来问，也不能只等 commit-trigger 或 heartbeat；该状态必须包含 Codex 是否仍在运行、是否有新 commit、最新验证结果、停止原因和下一步动作。
- 新增状态门禁语言：以后开发状态只允许写“已启动 / 启动失败 / 已结束”三种状态类型；只有确认 active executor session 存在后，才能写“Codex：运行中”或“下一步（已启动）”，否则必须写“Codex：未运行”或“待执行下一步”。同时，后台任务退出后必须先汇报，再做后续分析或重试。
- 采用 A + C 作为退出汇报硬方案：A = 后台 Codex 任务退出后，在给老板发状态前只允许先查新 commit 并整理状态消息；C = 退出汇报必须使用固定模板，明确写出 Codex 状态、状态类型、最新 commit、最新验证、停止原因和待执行下一步。
