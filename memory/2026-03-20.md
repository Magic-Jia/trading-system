- 已完成 ACP runtime backend 打通：本机启用了 `acpx` 扩展，用户态 `acpx` 可执行文件安装在 `/home/cn/.openclaw/tools/acpx/node_modules/.bin/acpx`，当前 OpenClaw 配置中 `plugins.entries.acpx.config.command` 指向该路径。
- 为了让主会话可读取 ACP 子会话结果，已确认并配置：`tools.sessions.visibility = "all"` 与 `tools.agentToAgent.enabled = true`；此前 `sessions_history` 被拒绝的根因就是这两项未打开。
- ACP 编码任务“空结果”的真实根因已查清：不是回传坏了，而是 `acpx` 在 non-interactive oneshot 会话中拦截 write/exec 权限请求。直接证据来自 `/home/cn/.openclaw/agents/codex/sessions/sessions.json` 的 `lastError`：`Permission denied by ACP runtime (acpx)... Configure plugins.entries.acpx.config.permissionMode ...`。
- 已把 `plugins.entries.acpx.config.permissionMode` 改为 `approve-all`；之后 ACP + Codex 编码任务不再因该权限错误空掉。
- ACP 路径当前能力边界：结果现在“可拉取/可见”（主会话能通过 `sessions_history` 读取 ACP 子会话结果），但没有验证出稳定的“completion 自动 push 回主会话”闭环；因此后续若依赖 ACP 做长任务，仍需主会话主动检查或外层可推送通道兜底。
- 成功的 ACP smoke test 会话：`agent:codex:acp:7d671dcd-3789-4fdf-8132-c79f5c540fff`，返回 `ACP_OK`，证明 backend/visibility 基本链路可用。
- 成功的 ACP 编码任务会话最终完成了真实开发并回传完整结果；产出 commit `cbacd2e815dc4f19ec2a40e8499f301ff999a677`（`feat: add execution outcome summary`），并报告测试通过：局部 `23 passed`、全量 `73 passed`。
- 这轮 ACP/Codex 新功能切片内容：给 v2 cycle 增加紧凑的 `execution_summary`，把 `FILLED / REPLAYED / BLOCKED / SKIPPED` 聚合到 runtime state / stdout / reporting，用于执行审计与运行期摘要。
- 重要流程教训：即使 ACP 结果已可读，主会话仍不会自动在任务完成时主动回用户；后续如果继续用 ACP 路径做开发，必须把“定时/节点性主动查询 ACP 子会话状态并立刻汇报”当成显式流程，而不能假设会有自动 completion push。
