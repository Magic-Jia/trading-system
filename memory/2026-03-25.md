# 2026-03-25

- 用户正式拍板新的策略主方向：从 `price-structure-first` 明确转向 `crypto-derivatives-aware`。
- 用户正式同意新的策略开发优先级：
  1. derivatives + crowding
  2. absolute strength + overheat
  3. regime crash protection
  4. edge-aware sizing + execution friction + turnover control
  5. richer stop taxonomy
  6. exit system
  7. short maturity
  8. strategy evaluation / ablation / attribution
- 用户正式同意：short 先不要提前成主线，继续排在 stop / exit 之后；crash protection 要前置；新增缺陷项（edge-aware sizing、execution friction、turnover / signal stability、alpha validation discipline）全部纳入主线。
- 策略方向文档已先由 Codex 重写，再由 Claw 手工二次修订补漏；主线已合入两笔文档提交：`ae7d760`（docs: rewrite strategy direction roadmap）与 `446f8d6`（docs: tighten strategy gaps and priorities）。
- 新的策略升级主文档包括：`trading_system/README.md`、`docs/superpowers/plans/2026-03-23-trading-system-p0-p1-p2-roadmap.md`、`trading_system/docs/MVP_ARCHITECTURE.md`、`trading_system/docs/STRATEGY_GAPS_AND_UPGRADES.md`。
- B1（crypto derivatives + crowding）实现计划已落地并提交：`a886ccf`（docs: add B1 derivatives crowding implementation plan）。
- B1 第一刀已由 Codex 落地并提交：`6f80cfd`（feat: filter trend candidates by derivatives crowding）；该切片已实现 trend engine 的 candidate-level derivatives 输入与 crowded-long 拒绝，聚焦测试通过，且当前无 blocker，可继续 Task 2。
- B1 在当天继续快速推进，后续已落地并提交多笔围绕 short / runtime / stdout / persisted-state 清理链路的窄切片：`4d0cf00`（trend derivatives metadata）、`05f2496`（rotation overheat filter）、`b3e0450`（short squeeze-risk filter）、`4e5f55d`（short candidate derivatives metadata）、`c424883`（downstream short derivatives metadata preservation）、`f5b5827`（main candidate generation passes derivatives）、`3b56233`（trend crowded-long main-cycle proof）、`314e6d7`（rotation crowded filter main-cycle proof）、`c0e3660`（short squeeze filter main-cycle proof）、`ea5134f`（short derivatives emitted report proof）、`f153eaa`（crowded short rejection stays out of stdout）、`528e3a6`（all-rejected short stdout empties correctly）、`db4ae95`（all-rejected short persisted summary stays empty）、`58b2006`（later rejected cycle clears stale short summary）、`cb12aac`（later rejected cycle clears stale short stdout）、`fdd5061`（short cleanup survives reload boundary）。
- 用户新增长期偏好：以后回复里如果用了英文单词，要在后面紧跟中文括号说明。
- 用户新增长期偏好：以后开发/验证汇报不要再直接报 pytest 测试函数名，改用人话概括“测了什么、结果怎样”。
- 用户新增长期要求：以后所有 direct acpx / Codex CLI 调用，默认都要显式设为 `openai-codex/gpt-5.4` + `high`，不要依赖模糊默认值。
- 用户明确要求把 acpx 正确语法记住；当天已确认本机 direct acpx 路径为 `/home/cn/.openclaw/tools/acpx/node_modules/.bin/acpx`，并确认 direct acpx 关键语法坑：`--cwd`、`--format`、`--approve-all`、`--model` 这类是全局参数，要放在 `codex` 前面；确认可用的一次性模板为：`acpx --cwd <worktree> --format quiet --approve-all --model openai-codex/gpt-5.4 codex exec "<prompt>"`。
- 当天对 Codex 持续开发的执行链做了路线调整：ACP runtime 多次出现 completion 空结果或不稳定退出后，后续老板场景下的持续编码任务开始优先切换到更可控的 direct acpx / persistent direct acpx 路径，并要求先做真实帮助输出预检再启动正式任务。
- 截至本次 compaction flush，B1 当前下一块仍在推进：验证同样的 short persisted + emitted cleanup，在“不同 state-load path / helper 重新加载持久化 accepted/deferred short 内容”后，后续 all-rejected 周期也依然能把旧 short 输出和状态清干净；该切片已通过 persistent direct acpx 会话启动，但结果尚未在主会话正式收口。
