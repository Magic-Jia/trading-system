# 2026-03-21

- 已确认 OpenClaw 实际生效配置文件路径为 `/home/cn/.openclaw/openclaw.json`（不是此前猜测的 `/home/cn/.openclaw/config.json`）。
- 已确认 live acpx 配置里 `plugins.entries.acpx.config.command` 指向 `/home/cn/.openclaw/tools/acpx/node_modules/.bin/acpx`，且该二进制存在、可执行、`--help` 可正常返回。
- 已确认 live acpx 配置原本已是 `permissionMode = approve-all`；后续为降低 non-interactive ACP 会话的直接失败风险，已手工把 `plugins.entries.acpx.config.nonInteractivePermissions` 补写为 `deny` 到 `/home/cn/.openclaw/openclaw.json`。
- 对“自动正式收口机制”排查的最新定位：repo 内侧 wrapper 已完成并提交（`a63924a`），当前剩余缺口在 repo 外的 launcher/orchestrator wiring；但继续追该外部链路前，一度被 ACP/acpx 的 non-interactive runtime 行为卡住。
- 从 `/home/cn/.openclaw/agents/codex/sessions/sessions.json` 已拿到明确历史证据：曾出现 `Permission denied by ACP runtime (acpx)... blocked a write/exec permission request in a non-interactive session`；后续若只显示 `acpx exited with code 1`，应优先怀疑同类 runtime/permission 问题的上层简化表现。
- 用户已协助确认：gateway 运行态最终已清回单实例；在此之后重新发起的最小 ACP probe（`agent:codex:acp:37fbe413-8b2d-437d-afbe-333432e35111`）不再像之前那样立即 `code 1` 秒退，而是进入 `identity.state = resolved`、`acp.state = running`。这说明“单 gateway + 更新后的 acpx 配置”至少已经压住了旧的立即失败模式。
- 用户新增状态表达规则：只要任务还在运行中，就不要说“有结果了”；此时只能明确写“还在跑 / 运行中”，最多补充当前阶段、卡点和下一步。只有在任务真正退出后，才能说“有结果 / 已结束 / 启动失败”。
- 用户进一步纠正状态表达边界：如果还没有真正启动任何 active executor / session / child run，就不能说“现在直接继续”“继续了”“在继续执行”；这类话只能在实际启动已被验证后使用。否则必须明确写“未运行 / 待执行下一步”。
